{
  "name": "alpha-video-player-workspace",
  "scripts": {
    "dev": "pnpm --parallel --filter=@pecasha/alpha-video-player --filter=offical dev",
    "build": "pnpm --parallel --filter=@pecasha/alpha-video-player --filter=offical build",
    "start": "pnpm --filter=offical start",
    "lint": "biome check . --fix"
  },
  "devDependencies": {
    "@empjs/biome-config": "^0.7.2",
    "typescript": "^5.4.5"
  }
}
